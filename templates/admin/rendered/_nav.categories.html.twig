<div class="row">

	<p class="d-flex justify-content-around">

		{% for category in categories %}

			<span class="d-inline">
				<a href="{{ path ('show_articles_from_categpry', {'alias': category.alias} ) }}" class="text-decoration-none text-warning">{{ category.name}}</a>
			</span>

		{% endfor %}
	</p>

</div>
